aiohttp>=3.9
psutil>=5.9
mss>=9.0
pillow>=10.0
pynput>=1.7
# Быстрый захват экрана на Windows-хосте (DXGI Desktop Duplication, 60+ FPS).
# Необязательно: без него работает запасной GDI-захват (~30 FPS).
dxcam; sys_platform == "win32"
numpy; sys_platform == "win32"
# Быстрое JPEG-кодирование на хосте (libjpeg-turbo, в 3-5 раз быстрее Pillow).
# Необязательно: без него используется Pillow.
opencv-python-headless; sys_platform == "win32"
# Готовый FFmpeg-бинарник; на RTX используется Desktop Duplication -> NVENC.
imageio-ffmpeg>=0.6; sys_platform == "win32"
